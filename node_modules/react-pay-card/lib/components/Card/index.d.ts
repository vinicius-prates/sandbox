import { LegacyRef } from 'react';
import './styles.css';
interface CardProps {
    cardHolder: string;
    cardNumber: string;
    cardMonth: string;
    cardYear: string;
    cardCvv: string;
    currentFocusedElm?: null | any;
    cardNumberRef?: LegacyRef<HTMLLabelElement> | undefined;
    cardHolderRef?: LegacyRef<HTMLLabelElement> | undefined;
    cardDateRef?: LegacyRef<HTMLLabelElement> | any;
}
declare const Card: ({ cardHolder, cardNumber, cardMonth, cardYear, cardCvv, currentFocusedElm, cardNumberRef, cardHolderRef, cardDateRef, }: CardProps) => JSX.Element;
export default Card;
